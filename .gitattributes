# Keep text files as LF in the repo and working tree so CI and GitHub Pages behave the same
# on the Linux runners regardless of the OS.
* text=auto eol=lf

# Binary assets - never apply end-of-line conversion to these.
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
*.hex  binary
*.bin  binary
